fastapi==0.115.*
uvicorn[standard]==0.30.*
pydantic-settings==2.*
google-auth==2.*
google-api-python-client==2.*
google-cloud-firestore==2.*
google-cloud-kms==2.*
# google-cloud-kms's generated client still imports pkg_resources at import
# time; setuptools>=81 dropped it, which breaks the app at boot. Pin until
# google-cloud-kms drops the pkg_resources usage upstream.
setuptools<81
cryptography==43.*
email-validator==2.*
mistune==3.*
# Runtime, not just test: app/services/calle_mcp.py talks to CALL-E's MCP
# server over plain HTTP. The Dockerfile installs this file only, so a
# runtime import that lives in requirements-dev.txt builds a clean image
# that dies at boot with ModuleNotFoundError. 0.27 also carries
# httpx.MockTransport, which is what the tests drive the client through --
# requirements-dev.txt deliberately adds no respx or pytest-mock.
httpx==0.27.*
